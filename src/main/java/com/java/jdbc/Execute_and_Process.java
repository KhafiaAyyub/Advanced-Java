package com.java.jdbc;

public class Execute_and_Process {
	
	
	
	//Connection con = DriverManager.getConncetion();  //create connection
	//	Statement st = con.createStatement();         // create statement
   // st.executeQuery(sql);                             //execute statement

	//data stored in result set-> it is also a interface 
	//rs.next() //this will give boolean value
	//true ->will get some data
	
	
	//it should also print connection based
	
	// improving the performance caching the queries - prepared statement
	
	//
	//lead to leaking of data to database
	//
}
